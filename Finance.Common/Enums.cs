namespace Finance.Common;

public enum PayType
{
    Salary = 0,
    Hourly = 1
}

public enum EmployeeStatus
{
    Active = 0,
    Inactive = 1,
    Terminated = 2
}

public enum PayrollRunStatus
{
    Draft = 0,
    Completed = 1,
    Closed = 2
}
